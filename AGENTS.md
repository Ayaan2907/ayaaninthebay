# agents

same rules for every coding agent (claude code, cursor, codex). read [`CLAUDE.md`](CLAUDE.md); it is the single source. nothing here overrides it.

short version: conventional commits with a scope, no ai co-author trailers, `process.env` only in `api/_env.js`, tests with behaviour changes, `npm run check && npm test` green, lowercase voice with no ai vocabulary.
